let media = 6
let faltas = 4
//6.5
if (media >= 7 && faltas < 5) {
    console.log(`Jovem APROVADO com média ${media} e com ${faltas} faltas`);
    } 
    
else if(media >= 6 && faltas <= 5) {
 console.log(`Jovem APROVADO pelo conselho com média ${media} e com ${faltas} faltas`);
}

else{
    console.log(`Jovem REPROVADO com média ${media} e com ${faltas} faltas`);
}
